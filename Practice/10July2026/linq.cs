// LINQ : language integrated query
// used to query collection like list array etc.
// where(), select(), orderBy(), orderByDescending()
// first(), count(), min(), max(), sum()

/* using System;
using System.Linq;


class Linq{
    public static void Main(){
        int[] num = {1,2,3,4,4,5,6,77,7,};
        var even = num.Where(x=>x%2==0);
        foreach(var n in even){
            Console.WriteLine(n);
        }

    }
} */